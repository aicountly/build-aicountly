# AICOUNTLY Build frontend (Vite)
#
# Vite bakes VITE_* into the JS bundle at build time.
# In production the value is set by the deploy pipeline; the file below
# is only for local development. Never commit a real .env.

VITE_API_URL=http://localhost:8080/api/v1
VITE_APP_NAME=AICOUNTLY Build
